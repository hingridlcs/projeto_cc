# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project overview

This is a minimal static HTML project. It currently consists of a single file, `index.html`, with no build tooling, package manager, dependencies, or test suite.

## Development

There is no build, lint, or test process. Open `index.html` directly in a browser to view it.
